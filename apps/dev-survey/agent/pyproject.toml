[project]
name = "survey-agent"
version = "0.1.0"
description = "FastAPI survey backend for the a2-react-aria developer survey"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-dotenv>=1.0.0",
]
