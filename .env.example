# Copy this file to .env.local and fill in real values.
# Never commit .env.local — it is gitignored.

# Required for the GitHub MCP server when using Claude Code standalone
# (not needed in VS Code — GitHub login handles auth automatically)
GITHUB_PERSONAL_ACCESS_TOKEN=
